export default function createrQuest (father) {
    const container_Quest = document.createElement('li');
    const checked = document.createElement('img');
    const close = document.createElement('img');

    container_Quest.setAttribute('class', )
}